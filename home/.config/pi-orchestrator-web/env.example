# Repo whose GitHub issues/orchestrator runs are exposed by the web UI.
ORCHESTRATOR_REPO=/path/to/repo

# Bind to localhost by default. Change to 0.0.0.0 only if you trust your LAN/firewall.
ORCHESTRATOR_HOST=127.0.0.1
ORCHESTRATOR_PORT=8787

PATH=~/.local/bin
