---
description: Generate a terminal-friendly Mermaid sequence diagram for a flow
argument-hint: "<flow>"
---
Create a concise sequence diagram for this flow:

$@

Use the sequence-diagrams skill. Return a brief explanation followed by one `mermaid` fenced `sequenceDiagram` block using only the skill's supported syntax. Do not invent missing behavior; state assumptions briefly.
